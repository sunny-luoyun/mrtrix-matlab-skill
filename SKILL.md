---
name: mrtrix-matlab-skill
description: >-
  MRtrix3 扩散磁共振图像处理 skill，覆盖数据整理、预处理、DTI、FOD/CSD、纤维追踪、FBA 纤维分析、网络矩阵构建、统计分析等完整流程。
  用户提及弥散/扩散磁共振/dMRI/DTI/DKI/FBA/FOD/纤维/MRtrix3 时自动触发。
---

# mrtrix-matlab 扩散磁共振图像处理技能

## 项目路径配置

`config.txt` 文件中配置了 `MRTRIX_MATLAB_HOME`。
开始处理任务前，先读取 `config.txt` 获取路径。

---

## 触发条件

当用户提及以下关键词时，自动触发本技能：
- 弥散/扩散磁共振/dMRI/DTI/DKI/FBA/FOD/纤维/Connectome
- 弥散像处理/扩散像/磁共振处理/mrview/mrconvert
- MRtrix/MRtrix3/fsl/ANTs
- 处理数据/跑流程/分析数据

## 项目简介

基于 MATLAB App Designer 的 MRtrix3 扩散磁共振图像处理工具。

**项目地址**：https://github.com/sunny-luoyun/mrtrix-matlab

使用本 skill 前，请先确保：
1. 已克隆或下载上述项目到本地
2. 已在本文件顶部的「项目路径配置」中填好了 `MRTRIX_MATLAB_HOME`
3. 已安装 MRtrix3、FSL、ANTs 等依赖工具
4. 已按项目 README 配置好环境（`cd MRTRIX_MATLAB_HOME` 后运行 `setup_path` 添加路径，编辑 `env.m` 设置各工具的本地安装路径）

**架构**：由 8 个功能模块组成，通过 `mrtrix`（主入口）-> `fun`（弥散像处理二级菜单）的 App Designer GUI 串联：
- `sort/` → 原始数据整理
- `pre/` → 预处理
- `dti/` → 弥散指标计算
- `fod/` → 反卷积响应函数计算
- `fiber/` → 纤维重建
- `fba/` → FBA 纤维分析（21 步标准流程）
- `map/` → 纤维网络矩阵构建
- `stats/` → 统计分析

---

## 核心交互流程

### 第一阶段：需求收集

通过对话了解用户的核心信息。按以下顺序询问：

1. **数据情况**
   - 数据格式是什么？（IMA/DICOM / NIFTI / .mif/.nii.gz）
   - 有多少个被试？样例会前缀是什么？
   - 数据是否已经按被试整理归类？
   - T1 结构像是否有？

2. **分析目标**
   列出选项让用户选择：
   - **DTI 指标计算** — FA/AD/RD/ADC/CL/CP/CS/AK/MK/RK，需要配准到 MNI
   - **FOD/CSD 反卷积** — 响应函数估计 + CSD + FOD 标准化
   - **纤维追踪** — 全脑追踪或 ROI 追踪 + SIFT 滤波 + TDI 图
   - **FBA 纤维分析** — 21 步标准流程（个体处理 → 模版构建 → 配准 → fixel 分析 → 统计）
   - **纤维网络矩阵** — 构建连接组矩阵
   - **统计分析** — 图像统计 / 簇统计 / 连接组统计 / 纤维统计
   - **仅整理数据** — 只做数据整理归类

3. **已处理情况**
   - 之前做过哪些处理步骤？
   - 是否有中间结果可用？

### 第二阶段：流程推荐

根据用户目标，推荐完整处理路径。模块依赖关系如下：

```
sort（数据整理）→ pre（预处理）→ fod（FOD 计算）→ fiber（纤维追踪）→ map（网络矩阵）
                                        ↓
                                       fba（FBA 分析，含自身预处理子流程）
                                        ↓
                                       stats（统计分析，所有模块最终输出均可进入）
                             dti（DTI 指标，依赖 pre 的输出）
```

**常见路径推荐**：

| 用户目标 | 推荐路径 | 说明 |
|---------|---------|------|
| DTI 指标 | `sort → pre → dti` | 预处理后直接算指标，可选配准到 MNI |
| FOD/CSD | `sort → pre → fod` | 响应函数 + CSD + 标准化 |
| 纤维追踪 | `sort → pre → fod → fiber` | 需先有 FOD |
| FBA | `sort → pre → fod → fba` | FBA 有自己的 21 步流程 |
| 网络矩阵 | `sort → pre → fod → fiber → map` | 需先有纤维追踪结果 |
| 统计分析 | `stats` 单独使用 | 依赖前序处理产生的数据 |
| 仅整理 | `sort` | 独立使用 |

### 第三阶段：分步引导

对每个推荐步骤，依次：

1. **说明该步骤的目的** — 用简单语言解释这一步在做什么
2. **询问关键参数** — 根据模块参考文档给出参数选项
3. **引导执行** — 提供两种方式任选：
   - **GUI 方式**：在 MATLAB 中先 `cd MRTRIX_MATLAB_HOME`，再运行对应的 App（如 `prepro` 打开预处理界面），指导用户填写参数
   - **函数方式**：直接给出需要调用的 MATLAB 函数及参数
4. **产出确认** — 说明这一步会生成什么文件/结果
5. **推进下一步** — 确认完成后询问是否继续下一步

### 第四阶段：注意事项提醒

在对话中自然嵌入以下规则：

- **首次使用**：必须先 `cd MRTRIX_MATLAB_HOME`，然后在 MATLAB 中运行 `setup_path` 添加路径，macOS 还需运行 `env.m` 设置环境变量
- **新增模块**：如用户需要新建模块文件夹，必须同步更新 `setup_path.m` 中的 `folders` 列表
- **数据备份**：预处理涉及数据覆盖性操作，建议先备份原始数据
- **MRtrix3 依赖**：部分功能依赖 MRtrix3/FSL/ANTs 命令行工具，需先在系统安装（各工具路径在 `env.m` 中配置）
- **耗时提醒**：头动校正（eddy）、FBA 模版构建、统计分析等步骤耗时较长，提醒用户合理安排

---

## 模块依赖关系详细说明

```
┌──────────────────────────────────────────────────────────────────┐
│  sort（数据整理）                                                │
│  输入: IMA/DICOM/NIFTI 原始数据                                  │
│  输出: 按被试整理的 DWI + T1                                     │
└──────────────┬───────────────────────────────────────────────────┘
               ▼
┌──────────────────────────────────────────────────────────────────┐
│  pre（预处理）                                                   │
│  输入: 按被试整理的 DWI + T1                                     │
│  步骤: 格式转换 → 去噪 → Gibbs → 头动校正 → B1校正 → 掩膜       │
│        → T1配准 → DWI配准到MNI → 组织分割                        │
│  输出: 预处理后的 .mif + 掩膜 + 配准矩阵 + 5tt                   │
└──────┬──────────────────────────┬───────────────────────────────┘
       │                          │
       ▼                          ▼
┌──────────────┐    ┌──────────────────────┐
│ dti（弥散指标）│    │ fod（FOD 反卷积）      │
│ 输入: 预处理   │    │ 输入: 预处理 DWI       │
│ DWI           │    │ 步骤: 响应函数 → CSD    │
│ 计算: DT/DKI   │    │       → 标准化 → MNI配准│
│ FA/AD/RD/...  │    │ 输出: FOD .mif         │
│ 输出: .nii指标  │    └──────────┬───────────┘
└──────────────┘               │
                               ▼
                    ┌──────────────────────┐
                    │ fiber（纤维重建）      │
                    │ 输入: FOD             │
                    │ 模式: 全脑 / ROI追踪   │
                    │ 可选: SIFT/SIFT2/TDI  │
                    │ 输出: .tck + .nii(TDI)│
                    └──────┬───────────────┘
                           │
                    ┌──────▼───────────────┐
                    │ map（网络矩阵构建）    │
                    │ 输入: .tck + 图谱    │
                    │ 输出: connectome 矩阵 │
                    └─────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│  fba（FBA 纤维分析）21 步标准流程                                 │
│  1-6: 个体处理（响应函数 → 平均 → 上采样 → 掩膜 → CSD → 标准化）│
│  7: 模版构建                                                    │
│  8-9: 配准（非线性配准 → 掩膜交集）                              │
│  10-16: Fixel 分析（fixel 掩膜 → FD/FC/FDC → 对应关系）         │
│  17-18: 纤维追踪 + 连接矩阵                                      │
│  19-20: 平滑 + 统计                                              │
│  21: 可视化                                                      │
└──────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│  stats（统计分析）                                                │
│  mrstats: 图像 ROI 统计                                          │
│  mrclusterstats: 置换检验簇推断                                  │
│  connectomestats: NBS/TFNBS 连接组统计                           │
│  tckstats: 纤维束统计                                            │
└──────────────────────────────────────────────────────────────────┘
```

---

## 常用 Shell 命令参考

当用户需要在终端而非 MATLAB 中操作时，常用的 MRtrix3 命令：

| 命令 | 用途 |
|------|------|
| `mrconvert` | 格式转换（DICOM/NIFTI ↔ .mif） |
| `mrview` | 图像可视化 |
| `dwi2mask` | 脑掩膜生成 |
| `dwidenoise` | 扩散去噪 |
| `dwifslpreproc` | 头动/涡流校正（含 eddy） |
| `dwi2tensor` | 张量拟合 |
| `tensor2metric` | 张量指标计算 |
| `dwi2response` | 响应函数估计 |
| `dwi2fod` | FOD 计算 |
| `tckgen` | 纤维追踪 |
| `tcksift` / `tcksift2` | SIFT 滤波/权重 |
| `tck2connectome` | 连接组矩阵 |
| `fixelcfestats` | Fixel 统计 |

这些命令来自 MRtrix3 工具包，需提前安装。各工具路径在项目 `env.m` 中配置，用户根据自身环境修改。

---

## 参考文档

各模块详细函数说明、参数表和注意事项，见 `references/` 目录：

- `references/pipeline-overview.md` — 整体流程决策树和快捷键
- `references/module-sort.md` — 数据整理模块
- `references/module-pre.md` — 预处理模块
- `references/module-dti.md` — DTI 指标计算模块
- `references/module-fod.md` — FOD 反卷积模块
- `references/module-fiber.md` — 纤维重建模块
- `references/module-fba.md` — FBA 纤维分析模块
- `references/module-map.md` — 网络矩阵构建模块
- `references/module-stats.md` — 统计分析模块
